import Background from "../Background";

const Cover = () => (
  <>
    <Background />
    <h1
      style={{
        width: "100%",
        textAlign: "center",
        color: "#fff",
        fontSize: "max(2rem, 6.5vw)",
        marginTop: "-.25em",
      }}
      className="pos-center"
    >
      Svg with me
    </h1>
  </>
);

export default Cover;
